2.Soru Cevap Soruyla

int main(){
    int x, y, z;
    x = 5; y = 10;
    z = ++x + y-- * 3;//++x önce çalışır y*3 sonra da --y çalışır;
    printf("x = %d\ny = %d\nz = %d\n", --x, y++, z);
    return 0;

    int x, y, z;
    x = 10; y = 6;
    z = x / y + 2 + x % y; // x/y xmod%y yapıcak 10/6 olucak sonra ++x olduğu için 11 olucak z 7 olucak;
    printf("%d %d\n", ++x, z++);
    return 0;
